<filter canvas.submission_comment_created>
  @type record_transformer
  enable_ruby
  <record>
submission_comment_id    "${record['body']['submission_comment_id']&.to_i}"
submission_id            "${record['body']['submission_id']&.to_i}"
user_id                  "${record['body']['user_id']&.to_i}"
created_at               "${record['body']['created_at']}"
attachment_ids           "${record['body']['attachment_ids'].join(',')}"
body                     "${record['body']['body']&.to_s}"
  </record>
</filter>
