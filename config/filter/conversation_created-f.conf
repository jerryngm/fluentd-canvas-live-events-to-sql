<filter canvas.conversation_created>
  @type record_transformer
  enable_ruby
  <record>
conversation_id  "${record['body']['conversation_id']&.to_i}"
updated_at       "${record['body']['updated_at']}"
  </record>
</filter>
