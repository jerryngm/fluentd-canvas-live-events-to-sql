<filter canvas.attachment_deleted>
  @type record_transformer
  enable_ruby
  <record>
attachment_id  "${record['body']['attachment_id']&.to_i}"
user_id        "${record['body']['user_id']&.to_i}"
display_name   "${record['body']['display_name']&.to_s}"
filename       "${record['body']['filename']&.to_s}"
folder_id      "${record['body']['folder_id']&.to_i}"
context_type   "${record['body']['context_type']&.to_s}"
context_id     "${record['body']['context_id']&.to_i}"
content_type   "${record['body']['content_type']&.to_s}"
unlock_at      "${record['body']['unlock_at']}"
lock_at        "${record['body']['lock_at']}"
updated_at     "${record['body']['updated_at']}"
  </record>
</filter>
