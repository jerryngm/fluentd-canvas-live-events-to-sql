<filter canvas.learning_outcome_result_updated>
  @type record_transformer
  enable_ruby
  <record>
learning_outcome_id  "${record['body']['learning_outcome_id']&.to_i}"
mastery              "${record['body']['mastery']&.to_s}" 
score                "${record['body']['score']&.to_f}"
created_at           "${record['body']['created_at']}"
attempt              "${record['body']['attempt']&.to_i}"
possible             "${record['body']['possible']&.to_f}"
original_score       "${record['body']['original_score']&.to_f}"
original_possible    "${record['body']['original_possible']&.to_f}"
original_mastery     "${record['body']['original_mastery']&.to_s}" 
assessed_at          "${record['body']['assessed_at']}"
title                "${record['body']['title']&.to_s}" 
percent              "${record['body']['percent']&.to_f}"
updated_at           "${record['body']['updated_at']}"
  </record>
</filter>
