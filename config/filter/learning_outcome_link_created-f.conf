<filter canvas.learning_outcome_link_created>
  @type record_transformer
  enable_ruby
  <record>
learning_outcome_link_id   "${record['body']['learning_outcome_link_id']&.to_i}"
learning_outcome_id        "${record['body']['learning_outcome_id']&.to_i}"
learning_outcome_group_id  "${record['body']['learning_outcome_group_id']&.to_i}"
context_id                 "${record['body']['context_id']&.to_i}"
context_type               "${record['body']['context_type']&.to_s}"
workflow_state             "${record['body']['workflow_state']&.to_s}"
  </record>
</filter>
