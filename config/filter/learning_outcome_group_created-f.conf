<filter canvas.learning_outcome_group_created>
  @type record_transformer
  enable_ruby
  <record>
learning_outcome_group_id  "${record['body']['learning_outcome_group_id']&.to_i}"
context_id                 "${record['body']['context_id']&.to_i}"
context_type               "${record['body']['context_type']&.to_s}"
title                      "${record['body']['title']&.to_s}"
description                "${record['body']['description']&.to_s}"
vendor_guid                "${record['body']['vendor_guid']&.to_s}"
parent_outcome_group_id    "${record['body']['parent_outcome_group_id']&.to_i}"
workflow_state             "${record['body']['workflow_state']&.to_s}"
  </record>
</filter>
