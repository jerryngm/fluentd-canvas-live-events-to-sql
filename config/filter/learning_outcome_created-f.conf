<filter canvas.learning_outcome_created>
  @type record_transformer
  enable_ruby
  <record>
learning_outcome_id      "${record['body']['learning_outcome_id']&.to_i}"
context_id               "${record['body']['context_id']&.to_i}"
context_type             "${record['body']['context_type']&.to_s}"
display_name             "${record['body']['display_name']&.to_s}"
short_description        "${record['body']['short_description']&.to_s}"
description              "${record['body']['description']&.to_s}"
vendor_guid              "${record['body']['vendor_guid']&.to_s}"
calculation_method       "${record['body']['calculation_method']&.to_s}"
calculation_int          "${record['body']['calculation_int']&.to_s}"
rubric_criterion_description     "${record['body']['rubric_criterion']['description']&.to_s}"
rubric_criterion_ratings         "${record['body']['rubric_criterion']['ratings']&.to_json.to_s}"
rubric_criterion_mastery_points  "${record['body']['rubric_criterion']['mastery_points']&.to_f}"
rubric_criterion_points_possible "${record['body']['rubric_criterion']['points_possible']&.to_f}"
title                            "${record['body']['title']&.to_s}"
workflow_state                   "${record['body']['workflow_state']&.to_s}"
  </record>
</filter>
