#doc: https://github.com/fluent/fluent-plugin-rewrite-tag-filter
<match canvas>
@type rewrite_tag_filter

<rule>
    key     metadata
    pattern /account_notification_created/
    tag canvas.account_notification_created
</rule>

<rule>
    key     metadata
    pattern /asset_accessed/
    tag canvas.asset_accessed
</rule>

<rule>
    key     metadata
    pattern /assignment_created/
    tag canvas.assignment_created
</rule>

<rule>
    key     metadata
    pattern /assignment_updated/
    tag canvas.assignment_updated
</rule>

<rule>
    key     metadata
    pattern /assignment_group_created/
    tag canvas.assignment_group_created
</rule>

<rule>
    key     metadata
    pattern /assignment_group_updated/
    tag canvas.assignment_group_updated
</rule>

<rule>
    key     metadata
    pattern /assignment_override_created/
    tag canvas.assignment_override_created
</rule>

<rule>
    key     metadata
    pattern /assignment_override_updated/
    tag canvas.assignment_override_updated
</rule>

<rule>
    key     metadata
    pattern /attachment_created/
    tag canvas.attachment_created
</rule>

<rule>
    key     metadata
    pattern /attachment_deleted/
    tag canvas.attachment_deleted
</rule>

<rule>
    key     metadata
    pattern /attachment_updated/
    tag canvas.attachment_updated
</rule>

<rule>
    key     metadata
    pattern /content_migration_completed/
    tag canvas.content_migration_completed
</rule>

<rule>
    key     metadata
    pattern /conversation_created/
    tag canvas.conversation_created
</rule>

<rule>
    key     metadata
    pattern /conversation_message_created/
    tag canvas.conversation_message_created
</rule>

<rule>
    key     metadata
    pattern /course_completed/
    tag canvas.course_completed
</rule>

<rule>
    key     metadata
    pattern /course_created/
    tag canvas.course_created
</rule>

<rule>
    key     metadata
    pattern /course_grade_change/
    tag canvas.course_grade_change
</rule>

<rule>
    key     metadata
    pattern /course_progress/
    tag canvas.course_progress
</rule>

<rule>
    key     metadata
    pattern /course_section_created/
    tag canvas.course_section_created
</rule>

<rule>
    key     metadata
    pattern /course_section_updated/
    tag canvas.course_section_updated
</rule>

<rule>
    key     metadata
    pattern /course_updated/
    tag canvas.course_updated
</rule>

<rule>
    key     metadata
    pattern /discussion_entry_created/
    tag canvas.discussion_entry_created
</rule>

<rule>
    key     metadata
    pattern /discussion_entry_submitted/
    tag canvas.discussion_entry_submitted
</rule>

<rule>
    key     metadata
    pattern /discussion_topic_created/
    tag canvas.discussion_topic_created
</rule>

<rule>
    key     metadata
    pattern /discussion_topic_updated/
    tag canvas.discussion_topic_updated
</rule>

<rule>
    key     metadata
    pattern /enrollment_created/
    tag canvas.enrollment_created
</rule>

<rule>
    key     metadata
    pattern /enrollment_state_created/
    tag canvas.enrollment_state_created
</rule>

<rule>
    key     metadata
    pattern /enrollment_state_updated/
    tag canvas.enrollment_state_updated
</rule>

<rule>
    key     metadata
    pattern /enrollment_updated/
    tag canvas.enrollment_updated
</rule>

<rule>
    key     metadata
    pattern /grade_change/
    tag canvas.grade_change
</rule>

<rule>
    key     metadata
    pattern /group_category_created/
    tag canvas.group_category_created
</rule>

<rule>
    key     metadata
    pattern /group_category_updated/
    tag canvas.group_category_updated
</rule>

<rule>
    key     metadata
    pattern /group_created/
    tag canvas.group_created
</rule>

<rule>
    key     metadata
    pattern /group_membership_created/
    tag canvas.group_membership_created
</rule>

<rule>
    key     metadata
    pattern /group_membership_updated/
    tag canvas.group_membership_updated
</rule>

<rule>
    key     metadata
    pattern /group_updated/
    tag canvas.group_updated
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_created/
    tag canvas.learning_outcome_created
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_group_created/
    tag canvas.learning_outcome_group_created
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_group_updated/
    tag canvas.learning_outcome_group_updated
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_link_created/
    tag canvas.learning_outcome_link_created
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_link_updated/
    tag canvas.learning_outcome_link_updated
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_result_created/
    tag canvas.learning_outcome_result_created
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_result_updated/
    tag canvas.learning_outcome_result_updated
</rule>

<rule>
    key     metadata
    pattern /learning_outcome_updated/
    tag canvas.learning_outcome_updated
</rule>

<rule>
    key     metadata
    pattern /logged_in/
    tag canvas.logged_in
</rule>

<rule>
    key     metadata
    pattern /logged_out/
    tag canvas.logged_out
</rule>

<rule>
    key     metadata
    pattern /module_created/
    tag canvas.module_created
</rule>

<rule>
    key     metadata
    pattern /module_item_created/
    tag canvas.module_item_created
</rule>

<rule>
    key     metadata
    pattern /module_item_updated/
    tag canvas.module_item_updated
</rule>

<rule>
    key     metadata
    pattern /module_updated/
    tag canvas.module_updated
</rule>

<rule>
    key     metadata
    pattern /plagiarism_resubmit/
    tag canvas.plagiarism_resubmit
</rule>

<rule>
    key     metadata
    pattern /quiz_export_complete/
    tag canvas.quiz_export_complete
</rule>

<rule>
    key     metadata
    pattern /quiz_submitted/
    tag canvas.quiz_submitted
</rule>

<rule>
    key     metadata
    pattern /quizzes.item_created/
    tag canvas.quizzes.item_created
</rule>

<rule>
    key     metadata
    pattern /quizzes.item_updated/
    tag canvas.quizzes.item_updated
</rule>

<rule>
    key     metadata
    pattern /quizzes-lti.grade_changed/
    tag canvas.quizzes-lti.grade_changed
</rule>

<rule>
    key     metadata
    pattern /quizzes_next_quiz_duplicated/
    tag canvas.quizzes_next_quiz_duplicated
</rule>

<rule>
    key     metadata
    pattern /quizzes.qti_import_completed/
    tag canvas.quizzes.qti_import_completed
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_clone_job_created/
    tag canvas.quizzes.quiz_clone_job_created
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_clone_job_updated/
    tag canvas.quizzes.quiz_clone_job_updated
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_created/
    tag canvas.quizzes.quiz_created
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_graded/
    tag canvas.quizzes.quiz_graded
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_session_submitted/
    tag canvas.quizzes.quiz_session_submitted
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_session_ungraded/
    tag canvas.quizzes.quiz_session_ungraded
</rule>

<rule>
    key     metadata
    pattern /quizzes.quiz_updated/
    tag canvas.quizzes.quiz_updated
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_session_created/
    tag canvas.quiz_caliper.quiz_session_created
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_session_updated/
    tag canvas.quiz_caliper.quiz_session_updated
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.asset_accessed/
    tag canvas.quiz_caliper.asset_accessed
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_created/
    tag canvas.quiz_caliper.quiz_created
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_started/
    tag canvas.quiz_caliper.quiz_started
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_submitted/
    tag canvas.quiz_caliper.quiz_submitted
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.quiz_updated/
    tag canvas.quiz_caliper.quiz_updated
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.item_created/
    tag canvas.quiz_caliper.item_created
</rule>

<rule>
    key     metadata
    pattern /quiz_caliper.item_updated/
    tag canvas.quiz_caliper.item_updated
</rule>

<rule>
    key     metadata
    pattern /sis_batch_created/
    tag canvas.sis_batch_created
</rule>

<rule>
    key     metadata
    pattern /sis_batch_updated/
    tag canvas.sis_batch_updated
</rule>

<rule>
    key     metadata
    pattern /submission_comment_created/
    tag canvas.submission_comment_created
</rule>

<rule>
    key     metadata
    pattern /submission_created/
    tag canvas.submission_created
</rule>

<rule>
    key     metadata
    pattern /submission_updated/
    tag canvas.submission_updated
</rule>

<rule>
    key     metadata
    pattern /syllabus_updated/
    tag canvas.syllabus_updated
</rule>

<rule>
    key     metadata
    pattern /user_account_association_created/
    tag canvas.user_account_association_created
</rule>

<rule>
    key     metadata
    pattern /user_created/
    tag canvas.user_created
</rule>

<rule>
    key     metadata
    pattern /user_updated/
    tag canvas.user_updated
</rule>

<rule>
    key     metadata
    pattern /wiki_page_created/
    tag canvas.wiki_page_created
</rule>

<rule>
    key     metadata
    pattern /wiki_page_deleted/
    tag canvas.wiki_page_deleted
</rule>

<rule>
    key     metadata
    pattern /wiki_page_updated/
    tag canvas.wiki_page_updated
</rule>
</match>
